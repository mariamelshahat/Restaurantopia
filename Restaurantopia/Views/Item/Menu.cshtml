@model IEnumerable<Restaurantopia.Entities.Models.Item>

@{
    ViewData["Title"] = "Menu";
    Layout = "_MenuPage";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">

@* <div class="row">
    <h1 class="col-md-6">Menu</h1>
    <p class="col-md-6">
        <a asp-action="Create" class="btn btn-success float-end">Create New</a>
    </p>
</div> *@

@* <div class="row" id="cards">
    <partial name="Cards" model="Model" />
</div>

 *@
@*
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.ItemTitle)</th>
            <th>@Html.DisplayNameFor(model => model.ItemDescription)</th>
            <th>@Html.DisplayNameFor(model => model.ItemImage)</th>
            <th>@Html.DisplayNameFor(model => model.ItemPrice)</th>
            <th>CategoryName</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Id)</td>
                <td>@Html.DisplayFor(modelItem => item.ItemTitle)</td>
                <td>@Html.DisplayFor(modelItem => item.ItemDescription)</td>
                <td>@Html.DisplayFor(modelItem => item.ItemImage)</td>
                <td>@Html.DisplayFor(modelItem => item.ItemPrice)</td>
                <td>
                    @foreach (var cat in ViewBag.C_s)
                    {
                        if (cat.Id == item.CategoryId)
                        {
                            @cat.CategoryName
                            break;
                        }
                    }
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
*@

 <div class="filters-content"> 
    <div class="row grid">
        @foreach (var item in Model)
        {
             <div class="col-sm-6 col-lg-4 all pizza"> 
                <div class="box">
                    <div class="img-box">
              @*   @if (item.ItemImage == null || item.ItemImage == "")
                {

                    <img src="\Images\ItemImage\no-image.webp" alt="NOImage" width="100%" height="100%" />
                }
                else
                {
                    <img src="@item.ItemImage" alt="NOImage" width="100%" height="100%" />
                }
            *@    <img src="@item.ItemImage" alt="Image for @item.ItemTitle">
                    </div>
                    <div class="detail-box">
                        <h5>@item.ItemTitle</h5>
                        <p>@item.ItemDescription</p>
                        <div class="options">
                            <h6>$@item.ItemPrice</h6>
                        </div><div class="options">
                        @Html.ActionLink("", "Edit", new { id = item.Id }, new { @class = "bi bi-pencil" }) |
                        @Html.ActionLink("", "Details", new { id = item.Id }, new { @class = "bi bi-eye" }) |
                        @Html.ActionLink("", "Delete", new { id = item.Id }, new { @class = "bi bi-trash" }) |
                        @Html.ActionLink("", "Order", new { id = item.Id }, new { @class = "bi bi-cart" })
                        </div>

                        
                        
                    </div>
                </div>
             </div> 
        }
    </div>
 </div> 
